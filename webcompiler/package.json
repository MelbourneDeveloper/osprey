{
  "name": "osprey-web-compiler",
  "version": "0.2.0",
  "description": "Secure web-based Osprey compiler with Monaco Editor and sandbox mode",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "./start.sh",
    "dev": "cd ../compiler && make clean && make build && cd ../webcompiler && node src/server.js",
    "server": "node src/server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "20.17.50"
  }
}
