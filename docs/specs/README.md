<!-- agent-pmo:74cf183 -->
# Osprey Specifications

This directory holds **all spec documents** for the project:

- **`0001-…` through `0019-…`** — the **Osprey language specification**: the
  authoritative reference for the language this compiler implements. These
  files are also published as the [Language Spec section](https://www.ospreylang.dev/spec/)
  of the website (generated by [`website/scripts/copy-spec.js`](../../website/scripts/copy-spec.js)
  — this README is explicitly skipped by that generator).
- **Engineering specs** (repo behaviour, processes, cross-cutting concerns) —
  added here as more arrive. Use the same `[GROUP-TOPIC]` ID convention.

Sibling [`../plans/`](../plans/) holds *plans* (how-to docs with TODO
checklists) rather than specs.

## Spec ID convention

Spec IDs are hierarchical descriptive slugs in the format `[GROUP-TOPIC]` or
`[GROUP-TOPIC-DETAIL]`. **NEVER** numbered IDs (`[SPEC-001]`). See the
project's [`CLAUDE.md`](../../CLAUDE.md) for the full convention.

Code implementing a spec section MUST reference its ID in a comment, e.g.
`// Implements [PARSER-EFFECTS-HANDLE]`. The `spec-check` skill enforces this
by grep.
