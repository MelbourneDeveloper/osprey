// This should fail compilation: Match expression must have at least one arm
// Tests: ErrMatchNoArms, WrapMatchNoArms
// Error: match expression must have at least one arm

type Option = Some { value: Int } | None

let option = Some { value: 42 }
let result = match option {
    // No arms provided
}
print(result)