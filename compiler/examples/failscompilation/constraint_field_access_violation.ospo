// COMPILATION FAILURE: Field Access on Non-Struct Types
// This demonstrates that field access is only allowed on record types

// Attempting field access on integer literal should fail
let x = 42
let result = x.value  // This should fail - integers don't have fields

print("This should not compile!") 