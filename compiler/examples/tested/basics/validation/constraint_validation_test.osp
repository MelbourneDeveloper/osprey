// BASIC TYPE CONSTRUCTION TEST  
// This tests basic type construction without validation for now

print("=== BASIC TYPE CONSTRUCTION TEST ===")

// Simple types without validation
type Person = {
    name: string,
    age: int 
}

type Product = {
    name: string,
    price: int
}

type Point = { x: int, y: int }

print("Test 1: Creating Person")
let person = Person { name: "Alice", age: 25 }
print("Person created")

print("\nTest 2: Creating Product")  
let product = Product { name: "Widget", price: 100 }
print("Product created")

print("\nTest 3: Creating Point")
let point = Point { x: 10, y: 20 }
print("Point created")

print("\n=== BASIC TYPE CONSTRUCTION TEST COMPLETE ===")
print("✅ All types created successfully")
print("📝 Note: Validation functions and field access to be added later") 